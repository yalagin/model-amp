apiVersion: v1
kind: Service
metadata:
  name: {{ include "models-amp.fullname" . }}
  labels:
    {{- include "models-amp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "models-amp.selectorLabels" . | nindent 4 }}
